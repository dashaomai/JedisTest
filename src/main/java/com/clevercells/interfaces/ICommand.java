package com.clevercells.interfaces;

/**
 * 用于存取的命令对象，可以批量发给对应的存取管理器类，执行对应的存取命令。
 *
 * 例如：发给 RedisManager，用于和 Redis 配合进行存取。
 *
 * Created by Bob Jiang on 2017/6/16.
 */
public interface ICommand {
}
